# Independent remediation review — round 5

Reviewed 2026-09-11 against the current working tree, following U01 and the
round-four runbook findings.

**Verdict: U01 is resolved. The reported available gates reproduce. No new
blocking implementation finding was identified in this targeted iteration.**
This closes the reviewed Swagger defect; it does not accept the entire
remediation, whose migration, generated contracts and integration/deployment
verification remain incomplete.

## Verified fixes

- CreateIncomeDto explicitly declares paymentDate as a nullable Swagger string
  with date format.
- UpdateIncomeDto explicitly exposes the same field, optional and nullable.
- Both new contract regressions pass (five tests in the contract suite).
- Independently generated Swagger from the freshly compiled DTOs in an empty
  Nest testing application. Both actual properties are `type: string`,
  `format: date`, `nullable: true`; paymentDate is not required in either schema.
  This used no database, application configuration or listening socket.
- The runbook now explicitly exits with failure when readiness times out, logs
  the failure and attempts cleanup. It captures the launched process PID and
  includes a final cleanup command. These controls were inspected, not executed
  against a running backend.

The contract tests check the actual generated schema rather than just TypeScript
types or decorator presence. Their claimed prior fail-first execution order was
not independently reconstructed. Broader pre-existing update-schema metadata
limitations are outside this focused paymentDate correction.

## Independently executed gates

All exit codes were taken directly from process results.

| Check | Exit | Result |
| --- | ---: | --- |
| `pnpm run build` | 0 | Backend builds |
| Installed Jest, coverage, `--runInBand` | 0 | 714 passed, 28 suites |
| Installed ESLint, backend workflow globs | 0 | No problems |
| Installed Vitest `run` in web | 0 | 587 passed, 2 skipped, 60 files |
| Installed ESLint `.` in web | 0 | 0 errors, 4 warnings |
| Frontend TypeScript `-b` | 2 | Same four stale shared BAS type errors |
| Root TypeScript `--noEmit --incremental false` | 2 | Same three unchanged-test diagnostics |
| Fresh `pnpm audit --json` | 1 | 0 critical/high, 2 moderate, 0 low |

The three root diagnostics remain app.controller.spec.ts (readonly isInitialized),
csv-import.service.spec.ts (recordsTotal), and the recurring-expenses controller
fixture (missing required fields). No new root compiler diagnostic appeared.
The audit still reports the Vitest/mocker moderate entries. The unchanged guard
suite was not rerun in this round; its prior independent result remains 7/7.

Tests/lint/type checks used installed runners directly. Backend Jest used
authorized temporary local HTTP binding. The build reported Node v24.21.0
against the declared Node 22 range; Node 22/CI parity remains unverified.

## Next acceptance boundary

Proceed to the disposable-database phase under the applicable operational
approval rules: generate/review the additive migration from the baseline schema,
test up/down/reapply, start the migrated backend and regenerate shared types,
then run the frontend build, backend integration and relevant Playwright flows.
Actual clean Docker builds/runtime and remote CI remain separate unverified
gates. Clean-install and historical audit evidence were not reconstructed here.

When executing the runbook, verify cleanup of the actual backend process:
the recorded PID belongs to the launched pnpm command, so child-process cleanup
should be checked rather than inferred from a successful `kill`. Do not declare
the integration lifecycle verified until the complete command exits naturally.

Only this report was added. Implementation files, dependencies and Git were not
mutated; no database or live service was contacted or started. Test/build
artifacts were generated by the verification commands.
